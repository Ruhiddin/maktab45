# Project Spec Status

`PROJECT-SPEC.md` is now the archived V1 specification entrypoint.

The active specification for current development, schema, auth, admin workflows, Excel import behavior, and V2 UX is:

- [V2-SPEC.md](/home/ruhiddin/Documents/Projects/school-leaderboard-fergana-45/V2-SPEC.md:1)

Use `V2-SPEC.md` as the source of truth for:

- named teacher accounts
- admin dashboard workflows
- Excel import formats
- audit log behavior
- archive flow
- public leaderboard V2 behavior

Keep this file as a pointer so older references to `PROJECT-SPEC.md` resolve to the current V2 spec instead of the deprecated V1 document.
